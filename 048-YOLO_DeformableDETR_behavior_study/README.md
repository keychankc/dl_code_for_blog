# YOLO vs Deformable DETR 行为对照实验

## 快速开始
```bash
pip install -r requirements.txt
 
python run_experiment.py --img_dir images --ann_path annotations/instances_val2017.json --sample_n 300 --device cpu
```
